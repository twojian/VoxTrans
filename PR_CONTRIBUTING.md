# PR 提交规范（Pull Request Guidelines）

> 本文档适用于所有参与本项目的开发者，请在提交 PR 前仔细阅读并严格遵守。

---

## 一、核心原则

**一个 PR，只做一件事。**

每个 PR 只实现或修改单一功能，鼓励尽可能小、粒度尽可能细的提交。大功能必须拆分为多个独立 PR 分步提交。

```
❌ 错误方式
└── PR: 完成整个用户系统

✅ 正确方式
├── PR1: 新增用户注册接口
├── PR2: 新增用户登录与 Token 鉴权
├── PR3: 新增用户信息展示页面
└── PR4: 新增密码重置功能
```

---

## 二、PR 标题格式

```
[类型] 一句话说明本 PR 新增/修改了什么
```

### 类型标签

| 标签 | 含义 |
|------|------|
| `feat` | 新增功能 |
| `fix` | 修复问题 |
| `refactor` | 代码重构（不影响功能） |
| `docs` | 文档更新 |
| `test` | 测试相关 |
| `chore` | 构建/配置/依赖调整 |
| `style` | 代码格式调整（不影响逻辑） |

### 示例

```
[feat] 新增用户登录模块，支持账号密码与手机验证码两种方式
[fix]  修复首页在移动端布局错位的问题
[docs] 更新 README 中的本地运行步骤
```

---

## 三、PR 描述模板

提交 PR 时，请按以下模板填写描述内容：

```markdown
## 功能描述
<!-- 说明该功能的作用与使用方式 -->
本 PR 实现了 _____ 功能。
用户可通过 _____ 方式使用，具体入口为 _____。

## 实现思路
<!-- 简要说明技术选型或核心实现逻辑 -->
- 技术选型：
- 核心逻辑：
- 关键文件/模块：

## 测试方式
<!-- 如何验证该功能正常运行 -->
1. 启动项目：`xxx`
2. 访问/执行：`xxx`
3. 预期结果：`xxx`

## 注意事项
- [ ] 是否引用了第三方库？（若是，请在 README 中列明依赖）
- [ ] 是否复用了已有代码片段？（若是，请在此注明来源）
- [ ] 合并后主分支是否仍可正常运行？
```

---

## 四、描述填写说明

### 4.1 功能描述
- 说明该功能解决了什么问题，或新增了什么能力
- 描述使用方式与操作入口，让评委无需阅读代码即可理解功能

### 4.2 实现思路
- 简要说明核心技术选型（如使用了哪个库/框架/算法）
- 描述关键实现逻辑，无需贴代码，一两句话即可
- 指出改动涉及的主要文件或模块

### 4.3 测试方式
- 提供可复现的具体步骤，如命令行指令或页面操作路径
- 说明预期输出或界面表现
- 若有测试用例，注明如何运行

---

## 五、合并要求

- 每次合并后，`main` / `master` 分支必须保持**可直接运行**状态
- 评委应能在任意时间 clone 仓库后，按 README 步骤**复现演示效果**
- PR 描述不得为空，且须与实际代码变更一致

---

## 六、无效 PR 情形（将被驳回）

| 情形 | 说明 |
|------|------|
| 描述为空或严重不符 | PR 描述与实际代码变更内容明显不符 |
| 未列明第三方依赖 | 引用了第三方库或框架，但未在 README 中说明 |
| 复用代码未注明来源 | 复用了自己过去的代码片段，未在 PR 描述中注明 |
| 一次性堆砌所有代码 | 仅在最后一天导入全部代码，缺乏持续提交记录 |
| commit 时间戳越界 | 提交时间不在所选批次的开始与截止时间之内 |

---

## 七、示例 PR（完整参考）

**标题：**
```
[feat] 新增 Markdown 实时预览功能，支持编辑器与预览区同步滚动
```

**描述：**

```markdown
## 功能描述
本 PR 为编辑器新增了 Markdown 实时预览功能。
用户在左侧输入 Markdown 文本时，右侧预览区将同步渲染 HTML 效果，
并支持两侧滚动位置联动。

## 实现思路
- 技术选型：使用 `marked.js` 解析 Markdown，`highlight.js` 处理代码高亮
- 核心逻辑：监听 textarea 的 input 事件，触发 marked.parse() 并更新 innerHTML；
  通过滚动比例计算实现双侧联动
- 关键文件：`src/components/Editor.vue`、`src/utils/markdownParser.js`

## 测试方式
1. 运行 `npm run dev` 启动本地服务
2. 打开 `http://localhost:5173`，进入编辑器页面
3. 在左侧输入 `# Hello World`，预期右侧实时显示对应 HTML 标题

## 注意事项
- [x] 引用了 `marked@9.1.6` 和 `highlight.js@11.9.0`，已在 README 依赖章节列明
- [x] 无复用历史代码
- [x] 合并后主分支可正常运行，已本地验证
```

---

*如有疑问，请在 Issue 中提出，或联系项目维护者。*
